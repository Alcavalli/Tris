namespace Constants
{
    //* Griglia
    constexpr int ROWS = 3, COLS = 3;
    constexpr int CELL_SIZE = 150;
    constexpr int LINES_WIDTH = 20;
    constexpr int LINES_LENGHT = CELL_SIZE * 3 + LINES_WIDTH * 2;

    //* Window
    constexpr int WINDOW_HEIGHT = CELL_SIZE * ROWS + 100, WINDOW_WIDTH = CELL_SIZE * COLS + 100;
}